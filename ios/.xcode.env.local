# This file is used to override the default NODE_BINARY path
# Customize for your local environment

# For nvm users
# export NODE_BINARY=$(command -v node)

# For Homebrew Node.js users
export NODE_BINARY=$(command -v node)

# For specific Node.js version path (uncomment and modify if needed)
# export NODE_BINARY=/usr/local/bin/node